- 跨包构建与检查通过根 package.json 的 scripts 统一编排，子包不暴露独立的全局命令。
- 版本变更使用 npm version --workspaces 配合 scripts/sync-versions.js 保持多工作区版本一致。
- 依赖锁定通过 scripts/check-pinned-deps.mjs 与 shrinkwrap 脚本强制校验，禁止未 pin 的依赖进入仓库。
- TypeScript 与 Vitest 配置集中在 tsconfig.base.json 与 vitest.base.ts，子包通过 extends 引用而非重复定义。